#include <stdio.h>
#include <main.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main() {

	name = "_putchar";
	
	for (int i=0; i<=name, i++)
	{
		printf (i);
		printf ("\n");
	return (0);
	}
